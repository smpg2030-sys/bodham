import shutil
import os

# Source is in artifacts (I need to find the specific path, usually it's in the current dir or I can list it)
# But generate_image saves to artifacts.
# I will list the artifacts dir first to find the exact name.

# Actually, I'll use the shell to move it.
